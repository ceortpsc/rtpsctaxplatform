# IRS-008 — OAuth/token (or approved auth) flow design reviewed for IRS API client

## Scope

Auth design review

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.IRS-008` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
