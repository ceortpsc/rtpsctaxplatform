# RFD-006 — Refund status data sources limited to authorized IRS/event channels

## Scope

Authorized source inventory

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.RFD-006` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
