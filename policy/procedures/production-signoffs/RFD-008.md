# RFD-008 — Operator tracking UI / API contracts reviewed for PII minimization

## Scope

API/UI privacy review

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.RFD-008` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
