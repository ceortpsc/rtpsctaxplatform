# INF-005 — Terraform prod population

## Scope

Populate `infra/terraform/env/prod` placeholders only after architecture,
security, and compliance sign-off.

## Evidence to attach

- Architecture approval reference:
- Modules / values populated:
- Approver name / role:
- Approval date (ISO-8601):

## Companion

See also `infra/terraform/env/prod/README.md`.

## Registry update

When approved, set `signoffs.INF-005` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`.
