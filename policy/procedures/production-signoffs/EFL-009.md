# EFL-009 — Production transmission kill-switch / disable path rehearsed

## Scope

SEV-1 disable-transmission drill record

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.EFL-009` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
