# AIA-008 — AI assist audit log retained for recommendations affecting compliance decisions

## Scope

Audit log retention config

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.AIA-008` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
