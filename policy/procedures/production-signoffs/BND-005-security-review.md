# BND-005 — Security review

## Scope

Secure tunnel, credential custody, certificate rotation, endpoint allowlists,
and data-handling controls before leaving stub mode.

## Evidence to attach

- Security review record ID:
- Tunnel / endpoint findings:
- Credential / secret-store findings:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.BND-005` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`.
