# OPS-006 — Enterprise tax checklist sign-offs complete (IRS, TDS, AI, refund, e-file)

## Scope

docs/enterprise-tax-software-checklist.md sign-off block

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.OPS-006` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
