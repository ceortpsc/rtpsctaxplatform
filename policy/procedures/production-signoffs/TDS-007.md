# TDS-007 — TDS job scheduling, retry, and escalation SLAs documented

## Scope

Operations runbook TDS section

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.TDS-007` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
