# GOV-008 — Enterprise AI assist policy approved (disclosure, retention, no unauthorized IRS use)

## Scope

AI assist policy + legal/security sign-off

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.GOV-008` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
