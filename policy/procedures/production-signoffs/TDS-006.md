# TDS-006 — Production TDS_CLIENT_ID provisioned in secret manager (not in VCS)

## Scope

Secret manager inventory

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.TDS-006` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
