# RFD-005 — Refund tracking timeline / case workflow design approved

## Scope

Refund tracking design + product sign-off

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.RFD-005` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
