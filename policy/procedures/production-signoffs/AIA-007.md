# AIA-007 — Taxpayer PII redaction rules enforced before any model prompt egress

## Scope

PII redaction test evidence

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.AIA-007` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
