# AIA-005 — Human-in-the-loop required for AI recommendations affecting filings or refunds

## Scope

AI assist operating procedure

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.AIA-005` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
