# TDS-008 — TDS sandbox pull validated before production enablement

## Scope

Sandbox TDS validation report

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.TDS-008` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
