# OPS-004 — Operations sign-off

## Scope

Worker scheduling, `--once` replay procedures, and incident playbook readiness
for SEV-1/2/3.

## Evidence to attach

- Playbook review reference:
- Workers covered (TDS / transcript-pull / live-source):
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.OPS-004` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`.
