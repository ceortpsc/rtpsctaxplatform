# RFD-007 — Refund intelligence scoring validated against sample authorized events

## Scope

Validation dataset report

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.RFD-007` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
