# GOV-006 — Data governance review

## Scope

Taxpayer data retention, masking, redaction, and operator-visible metadata rules
for live exchange.

## Evidence to attach

- Review record ID:
- Retention window:
- Masking / redaction controls:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.GOV-006` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`.
