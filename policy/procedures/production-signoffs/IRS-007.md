# IRS-007 — Production API_CLIENT_ID rotated and stored only in approved secret manager

## Scope

Secret manager inventory + rotation log

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.IRS-007` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
