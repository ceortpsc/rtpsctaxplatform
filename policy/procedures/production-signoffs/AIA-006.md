# AIA-006 — External LLM / model vendor DPIA and BAA (if applicable) completed

## Scope

DPIA / vendor risk package

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.AIA-006` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
