# TDS-005 — TDS client enrollment and transmitter agreements approved

## Scope

TDS enrollment / agreement package

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.TDS-005` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
