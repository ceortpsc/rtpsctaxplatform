# IRS-005 — IRS API client enrollment / e-Services (or successor) approval recorded

## Scope

IRS enrollment / client-id issuance record

## Evidence to attach

- Review / approval record ID:
- Supporting artifacts:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.IRS-005` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`. Never forge approvals.
