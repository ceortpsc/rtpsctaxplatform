# CFG-005 — Runtime environment confirmation

## Scope

Confirm live deploy runtime reports `APP_ENV=prod` and `NODE_ENV=production`
via `/metadata` (redacted) after cutover.

## Evidence to attach

- Deploy ID / release tag:
- `/metadata` environment fields observed:
- Approver name / role:
- Approval date (ISO-8601):

## Registry update

When approved, set `signoffs.CFG-005` in `registry.json` to `status: "approved"`
with `approver` and `approvedAt`.
