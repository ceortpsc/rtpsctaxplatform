# `@rtp/secrets-config`

Catalog and redacted readiness evaluation for platform environment secrets.

Groups: `api`, `tds`, `tunnel`, `session`, `encryption`, `sbtpg`, `irs`, `tls`.

Never returns raw secret values — only presence, length hints, and missing keys.
