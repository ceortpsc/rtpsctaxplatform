# Staging Agent

_Ross Tax Pro Software Co — Efile Transmission Software_

A 6-stage promotion pipeline; live transmission is gated (currently: PROTECTED).

> Generated by `staging-agent` at 2026-07-24T21:16:55.278Z.

## Staging pipeline

| Stage | Components | Gate |
| --- | --- | --- |
| Stage 0 — Build | 4 | All quality gates green |
| Stage 1 — Shared runtime | 5 | Package tests pass |
| Stage 2 — Processing | 9 | Workflows validated |
| Stage 3 — Workers | 4 | Background runner healthy |
| Stage 4 — Services | 6 | All /health endpoints OK (deploy:all) |
| Stage 5 — Promotion | 4 | Environment protection satisfied before prod e-file transmission |

## Stage 0 — Build

- Components: pnpm install, pnpm run lint, pnpm test, pnpm run build
- Gate: All quality gates green

## Stage 1 — Shared runtime

- Components: @rtp/platform-core, @rtp/client-config, @rtp/secure-tunnel, @rtp/workflow-engine, @rtp/bank-products
- Gate: Package tests pass

## Stage 2 — Processing

- Components: refund-intelligence-engine, analytics-center, tc-code-engine, transmission-pipeline, masterfile-pipeline, refund-status-pipeline, refund-status-update, transcript-intake, transmission-cycle
- Gate: Workflows validated

## Stage 3 — Workers

- Components: tds-worker, transcript-pull-worker, live-source-fetcher, workflow-runner
- Gate: Background runner healthy

## Stage 4 — Services

- Components: api-gateway, refund-status-service, transcript-service, analytics-service, enrollment-service, modules-dashboard
- Gate: All /health endpoints OK (deploy:all)

## Stage 5 — Promotion

- Components: local, dev, stage, prod
- Gate: Environment protection satisfied before prod e-file transmission

## Deploy command

```bash
pnpm run deploy:all   # health-checks every service + background worker
```
