# XHTML / XML assets

Approved practitioner payload fixtures and generated shapes:

- `practitioner/account.fixture.xml` — Tax Practitioner account
- Runtime builders: `@rtp/irs-xml` (`buildPractitionerAccountXml`, `buildMasterfileRectificationXml`, `buildRefundReleaseRequestXml`, `buildRefundReconciliationXml`, `buildPractitionerSuiteXhtml`)

Live MeF transmission is not performed from these templates.
