# PDF Fill Engine (Python scaffold)

Deterministic PDF form-fill stub for RTPSC. Install dependencies from the repo root:

```bash
python3 -m pip install -r requirements.txt
python3 engines/pdf-fill-engine/src/fill.py --help
```

This module does not call IRS systems. Production fill + signing requires approved credentials and operational sign-off.
